<UserControl x:Class="BatoBuzz.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="860">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360"/>
            <ColumnDefinition Width="500"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{DynamicResource ShellHeaderBrush}" CornerRadius="8,0,0,8" Padding="34">
            <StackPanel VerticalAlignment="Center">
                <Border Width="48" Height="48" CornerRadius="8" Background="{DynamicResource PrimaryBrush}" HorizontalAlignment="Left" Margin="0,0,0,22">
                    <TextBlock Text="B" Foreground="{DynamicResource WhiteBrush}" FontSize="27" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="BatoBuzz Accounting" FontSize="25" FontWeight="SemiBold" Foreground="{DynamicResource WhiteBrush}" TextWrapping="Wrap" Margin="0,0,0,12"/>
                <TextBlock Text="Offline-first business accounts with sales, purchase, inventory, journals, reports, backup and restore." Foreground="{DynamicResource ShellHeaderMutedBrush}" TextWrapping="Wrap" LineHeight="21"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,1,1" CornerRadius="0,8,8,0" Padding="42">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Sign in" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4"/>
                <TextBlock Text="Use your local BatoBuzz account" FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,28"/>

                <TextBlock Text="Username" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                <TextBox Text="{Binding UserName}" Margin="0,0,0,16"/>

                <TextBlock Text="Password" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_OnPasswordChanged" Margin="0,0,0,8"/>

                <CheckBox Content="Remember me on this computer"
                          IsChecked="{Binding RememberMe}"
                          Margin="0,0,0,12"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource ErrorBrush}" TextWrapping="Wrap" Margin="0,0,0,16" FontSize="12"/>

                <Button Content="{Binding LoginButtonText}" Command="{Binding LoginCommand}" Style="{DynamicResource PrimaryButton}" Margin="0,0,0,8"/>
                <Button Content="Local Login" Command="{Binding LoginOfflineCommand}" Style="{DynamicResource SecondaryButton}" Margin="0,0,0,16"/>

                <TextBlock Text="Forgot password?" Foreground="{DynamicResource PrimaryBrush}" HorizontalAlignment="Center" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
