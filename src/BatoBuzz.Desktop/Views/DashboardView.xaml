<UserControl x:Class="BatoBuzz.Desktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
        <Grid MaxWidth="1480" Margin="4,2,4,16" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Page heading -->
            <Border Grid.Row="0" Style="{DynamicResource SectionPanel}" Padding="22,18" Margin="0,0,0,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Financial overview" FontSize="25" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="A live snapshot of " Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock Text="{Binding DataContext.CompanyName, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="{DynamicResource PrimaryDarkBrush}" FontWeight="SemiBold"/>
                            <TextBlock Text=" for the current month" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button Grid.Column="1"
                            Command="{Binding RefreshCommand}"
                            Style="{DynamicResource SecondaryButton}"
                            Padding="16,9"
                            VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="↻" FontSize="17" Margin="0,-2,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Refresh data" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- Service errors are visible without taking permanent space. -->
            <Border Grid.Row="1" Background="#FFF4F4" BorderBrush="#F2B8B8" BorderThickness="1" CornerRadius="9" Padding="13,10" Margin="0,0,0,12">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding StatusMessage}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="!" Foreground="{DynamicResource ErrorBrush}" FontWeight="Bold" Margin="0,0,9,0"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource ErrorBrush}" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Key business metrics -->
            <Grid Grid.Row="2" Margin="-6,0,-6,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource KpiCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{DynamicResource AccentBrush}" CornerRadius="12,0,0,12"/>
                        <StackPanel Grid.Column="1" Margin="17,14">
                            <DockPanel LastChildFill="True">
                                <Border DockPanel.Dock="Right" Background="#E8F0FF" CornerRadius="12" Padding="8,3">
                                    <TextBlock Text="CASH" Foreground="{DynamicResource AccentBrush}" FontSize="9" FontWeight="Bold"/>
                                </Border>
                                <TextBlock Text="Cash balance" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </DockPanel>
                            <TextBlock Text="{Binding CashBalance, StringFormat='Rs. {0:N2}'}" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="20" FontWeight="SemiBold" Margin="0,9,0,3"/>
                            <TextBlock Text="Available on hand" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="0" Grid.Column="1" Style="{DynamicResource KpiCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{DynamicResource PrimaryBrush}" CornerRadius="12,0,0,12"/>
                        <StackPanel Grid.Column="1" Margin="17,14">
                            <DockPanel LastChildFill="True">
                                <Border DockPanel.Dock="Right" Background="{DynamicResource PrimarySoftBrush}" CornerRadius="12" Padding="8,3">
                                    <TextBlock Text="BANK" Foreground="{DynamicResource PrimaryDarkBrush}" FontSize="9" FontWeight="Bold"/>
                                </Border>
                                <TextBlock Text="Bank balance" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </DockPanel>
                            <TextBlock Text="{Binding BankBalance, StringFormat='Rs. {0:N2}'}" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="20" FontWeight="SemiBold" Margin="0,9,0,3"/>
                            <TextBlock Text="Across bank ledgers" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="0" Grid.Column="2" Style="{DynamicResource KpiCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{DynamicResource SuccessBrush}" CornerRadius="12,0,0,12"/>
                        <StackPanel Grid.Column="1" Margin="17,14">
                            <DockPanel LastChildFill="True">
                                <Border DockPanel.Dock="Right" Background="#E8F7ED" CornerRadius="12" Padding="8,3">
                                    <TextBlock Text="SALES" Foreground="{DynamicResource SuccessBrush}" FontSize="9" FontWeight="Bold"/>
                                </Border>
                                <TextBlock Text="Monthly sales" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </DockPanel>
                            <TextBlock Text="{Binding TotalSalesMonth, StringFormat='Rs. {0:N2}'}" Foreground="{DynamicResource SuccessBrush}" FontSize="20" FontWeight="SemiBold" Margin="0,9,0,3"/>
                            <TextBlock Text="Posted this month" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="0" Grid.Column="3" Style="{DynamicResource KpiCard}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{DynamicResource WarningBrush}" CornerRadius="12,0,0,12"/>
                        <StackPanel Grid.Column="1" Margin="17,14">
                            <DockPanel LastChildFill="True">
                                <Border DockPanel.Dock="Right" Background="#FFF2E3" CornerRadius="12" Padding="8,3">
                                    <TextBlock Text="BUY" Foreground="{DynamicResource WarningBrush}" FontSize="9" FontWeight="Bold"/>
                                </Border>
                                <TextBlock Text="Monthly purchases" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            </DockPanel>
                            <TextBlock Text="{Binding TotalPurchasesMonth, StringFormat='Rs. {0:N2}'}" Foreground="{DynamicResource WarningBrush}" FontSize="20" FontWeight="SemiBold" Margin="0,9,0,3"/>
                            <TextBlock Text="Posted this month" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource KpiCard}">
                    <StackPanel Margin="17,14">
                        <TextBlock Text="Receivables" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding TotalReceivables, StringFormat='Rs. {0:N2}'}" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="20" FontWeight="SemiBold" Margin="0,9,0,3"/>
                        <TextBlock Text="{Binding OutstandingInvoices, StringFormat='{}{0} invoices awaiting payment'}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Grid.Column="1" Style="{DynamicResource KpiCard}">
                    <StackPanel Margin="17,14">
                        <TextBlock Text="Payables" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding TotalPayables, StringFormat='Rs. {0:N2}'}" Foreground="{DynamicResource ErrorBrush}" FontSize="20" FontWeight="SemiBold" Margin="0,9,0,3"/>
                        <TextBlock Text="{Binding OutstandingBills, StringFormat='{}{0} bills awaiting payment'}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Grid.Column="2" Style="{DynamicResource KpiCard}">
                    <StackPanel Margin="17,14">
                        <TextBlock Text="Gross profit" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding GrossProfitMonth, StringFormat='Rs. {0:N2}'}" Foreground="{DynamicResource SuccessBrush}" FontSize="20" FontWeight="SemiBold" Margin="0,9,0,3"/>
                        <TextBlock Text="Current month performance" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Grid.Column="3" Style="{DynamicResource KpiCard}">
                    <StackPanel Margin="17,14">
                        <TextBlock Text="Low stock" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding LowStockItemsCount}" Foreground="{DynamicResource ErrorBrush}" FontSize="20" FontWeight="SemiBold" Margin="0,9,0,3"/>
                        <TextBlock Text="Items need attention" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Common work and reports -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{DynamicResource SectionPanel}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="Daily work" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="17" FontWeight="SemiBold"/>
                            <TextBlock Text="Start the transactions you use most" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" Margin="0,3,0,0"/>
                        </StackPanel>
                        <UniformGrid Grid.Row="1" Columns="2" Rows="3">
                            <Button Command="{Binding DataContext.ShowSalesInvoiceCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{DynamicResource ActionTileButton}" Margin="0,0,6,6" Background="{DynamicResource PrimarySoftBrush}" BorderBrush="{DynamicResource PrimaryBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <Border Width="32" Height="32" CornerRadius="9" Background="{DynamicResource PrimaryBrush}"><TextBlock Text="S" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Text="Sales invoice" FontWeight="SemiBold"/><TextBlock Text="Record a customer sale" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11"/></StackPanel>
                                </Grid>
                            </Button>
                            <Button Command="{Binding DataContext.ShowPurchaseBillCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{DynamicResource ActionTileButton}" Margin="6,0,0,6">
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <Border Width="32" Height="32" CornerRadius="9" Background="#FFF2E3"><TextBlock Text="P" Foreground="{DynamicResource WarningBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Text="Purchase bill" FontWeight="SemiBold"/><TextBlock Text="Capture a supplier bill" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11"/></StackPanel>
                                </Grid>
                            </Button>
                            <Button Command="{Binding DataContext.ShowReceiptCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{DynamicResource ActionTileButton}" Margin="0,6,6,6">
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <Border Width="32" Height="32" CornerRadius="9" Background="#E8F7ED"><TextBlock Text="R" Foreground="{DynamicResource SuccessBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Text="Receipt" FontWeight="SemiBold"/><TextBlock Text="Receive customer payment" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11"/></StackPanel>
                                </Grid>
                            </Button>
                            <Button Command="{Binding DataContext.ShowPaymentCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{DynamicResource ActionTileButton}" Margin="6,6,0,6">
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <Border Width="32" Height="32" CornerRadius="9" Background="#FFF0F0"><TextBlock Text="₹" Foreground="{DynamicResource ErrorBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Text="Payment" FontWeight="SemiBold"/><TextBlock Text="Pay a supplier or expense" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11"/></StackPanel>
                                </Grid>
                            </Button>
                            <Button Command="{Binding DataContext.ShowJournalEntryCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{DynamicResource ActionTileButton}" Margin="0,6,6,0">
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <Border Width="32" Height="32" CornerRadius="9" Background="#E8F0FF"><TextBlock Text="J" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Text="Journal entry" FontWeight="SemiBold"/><TextBlock Text="Post a general journal" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11"/></StackPanel>
                                </Grid>
                            </Button>
                            <Button Command="{Binding DataContext.ShowInventoryCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{DynamicResource ActionTileButton}" Margin="6,6,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <Border Width="32" Height="32" CornerRadius="9" Background="#F0EDFF"><TextBlock Text="I" Foreground="#6D4BD1" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock Text="Inventory" FontWeight="SemiBold"/><TextBlock Text="Items and stock movement" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11"/></StackPanel>
                                </Grid>
                            </Button>
                        </UniformGrid>
                    </Grid>
                </Border>

                <Border Grid.Column="2" Style="{DynamicResource SectionPanel}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="Financial reports" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="17" FontWeight="SemiBold"/>
                            <TextBlock Text="Review and reconcile" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" Margin="0,3,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1">
                            <Button Content="Trial Balance  →" Command="{Binding DataContext.ShowReportCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="Trial Balance" Style="{DynamicResource ActionTileButton}" Height="60" Margin="0,0,0,8" HorizontalContentAlignment="Left"/>
                            <Button Content="Profit &amp; Loss  →" Command="{Binding DataContext.ShowReportCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="Profit &amp; Loss" Style="{DynamicResource ActionTileButton}" Height="60" Margin="0,0,0,8" HorizontalContentAlignment="Left"/>
                            <Button Content="Balance Sheet  →" Command="{Binding DataContext.ShowReportCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="Balance Sheet" Style="{DynamicResource ActionTileButton}" Height="60" HorizontalContentAlignment="Left"/>
                        </StackPanel>
                        <Button Grid.Row="2" Content="Open report center" Command="{Binding DataContext.ShowReportsCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{DynamicResource PrimaryButton}" Margin="0,14,0,0"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
