<UserControl x:Class="BatoBuzz.Desktop.Views.PeriodLockSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Period Lock" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,12">
            Nothing may be posted or dated on or before the lock date below — use this once a
            period has been filed and reviewed, to prevent accidental backdating into it. This
            blocks Sales Invoices, Purchase Bills, Receipts, Payments, Journal Entries, Credit
            Notes, and Debit Notes.
        </TextBlock>

        <Border Grid.Row="2" Style="{DynamicResource SectionPanel}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0">
                    <TextBlock Text="Lock Date" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11"/>
                    <DatePicker SelectedDate="{Binding LockDate}" Width="160"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="2" VerticalAlignment="Bottom">
                    <Button Content="Save" Command="{Binding SaveCommand}" Style="{DynamicResource PrimaryButton}" Padding="16,6"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="4" VerticalAlignment="Bottom">
                    <Button Content="Clear Lock" Command="{Binding ClearCommand}" Style="{DynamicResource SecondaryButton}" Padding="16,6"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="6" Orientation="Horizontal" Margin="0,14,0,0">
                    <TextBlock Text="Currently locked through: " Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <TextBlock Text="{Binding CurrentLockDate, StringFormat='{}{0:yyyy-MM-dd}', TargetNullValue='(none)'}" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                </StackPanel>
                <TextBlock Grid.Row="2" Grid.ColumnSpan="6" Text="{Binding StatusMessage}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,8,0,0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
